import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ApplyForCard = () => {
    return (
        <View>
            <Text>ApplyForCard</Text>
        </View>
    )
}

export default ApplyForCard

const styles = StyleSheet.create({})